structure (Results_bus){
property (RESULT){
type(RESULT)
    min(Optional) max(Many)
}
property(BUSSTOP_LIST){
    type(BUSSTOP_LIST)
    min(Optional) max(Many)
  }
  property(ROW_COUNT){
    type(ROW_COUNT)
    min(Optional) max(Many)
  }
}